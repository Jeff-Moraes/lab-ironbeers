<div class="container-beers">
  {{#each beer}}
  <div>
    {{!-- <img src="{{this.image_url}}" alt="">
    <div>
      <h3>{{this.name}}</h3>
      <h6>{{this.tagline}}</h6>
      <p>{{this.description}}</p>
    </div> --}}
    {{> beerpartials }}
    <a href="/beer/{{this.id}}">DETAILS</a>
  </div>
  {{/each}}
</div>